This subdirectory contains all parts for the report.

To build the main.tex properly (s.t. the output is put into the respective build forlder):
enter Terminal, cd to /root/report and use latexmk -pdf main.tex